package com.nike.elements;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

public class CarouselItem extends BaseElements{
    public CarouselItem(WebDriver driver, WebElement root) {
        super(driver, root);
    }
}
